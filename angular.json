{
  "$schema": "./node_modules/nx/schemas/workspace-schema.json",
  "version": 2,
  "projects": {
    "api": "apps/api",
    "app": "apps/app",
    "client": "apps/client",
    "kitchen": "apps/kitchen",
    "models": "libs/models"
  }
}
